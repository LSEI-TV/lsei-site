---
name: lsei-new-site
description: Refonte du site lsei.tv — emplacement du dev et périmètre (5 disciplines)
metadata: 
  node_type: memory
  type: project
  originSessionId: da21c2bc-82e6-4514-9030-dcc992a759ea
---

Refonte de la web TV **LSEI — Le Sport en Image** (lsei.tv). Le développement vit dans `D:\LSEI - NEW SITE` (dossier séparé de `D:\BASKET`), car le site couvre les **5 disciplines** (basket NM1, billard, palets bretons, football, bowling), pas seulement le basket.

**Why:** décision explicite du 2026-07-12 : ne pas mélanger le site multi-sports avec le projet basket-live.
**How to apply:** créer/éditer les fichiers du site dans `D:\LSEI - NEW SITE`. Page d'accueil actuelle : `index.html`.

Direction validée : style **clair & éditorial**, accent cobalt `#1E40E5`, rouge live `#E5342B`. Pas d'intégration du module basket-live. Sans lien avec [[basket-live-architecture]].

**Stack (validée 2026-07-12) :** site statique **Astro** (`npm run dev` → port 4321) + **synchro YouTube** au build. La maquette HTML d'origine est conservée dans `reference/maquette.html`.

**Structure :** `src/data/site.ts` (disciplines, partenaires, stats, textes = contenu fixe éditable) · `src/lib/youtube.ts` (couche vidéo, **mock pour l'instant**, bascule sur l'API réelle dès que `YOUTUBE_API_KEY` + `YOUTUBE_CHANNEL_ID` dans `.env`) · composants `Thumb/Header/Footer/Ticker` · `layouts/Base.astro` · pages `index`, `disciplines/[slug]`, `videotheque`, `contact`.

**Modèle de contenu voulu :** 5 playlists YouTube (1/discipline) → pages disciplines + vidéothèque ; live/à-programme détectés via l'API ; le reste maintenu dans `site.ts`. Geste unique de l'asso : publier sur YouTube + ranger dans la bonne playlist.

**Chaîne réelle :** « LSEI Web Tv Sport » (@LSEIWebTvSport), channel ID `UCYUOdKr28cOjLzHWv6pTpjQ` (pré-rempli dans `.env`/`.env.example`), ~14,9 k abonnés, ~2 300 vidéos. Playlists organisées par **compétition** (pas par discipline) → classement auto par mots-clés dans `classifyPlaylist()` (`youtube.ts`), « LES LIVES » ignorée. Manque : la clé `YOUTUBE_API_KEY` (l'utilisateur doit la créer et la coller dans `.env`).

**7 disciplines** (2026-07-15) : basket, billard, palets, **football** (ponctuel, Ligue de Bretagne), **beach-volley**, bowling, **subbuteo** (Coupe du Monde 2026, contenu à venir). Grille `.discs` en `repeat(4)`. Compteur `count` des disciplines n'est plus affiché (ligne `.cnt` retirée des cartes). Football câblé avec 12 vraies vidéos (finales Coupe Région Bretagne 2025-2026) via `FOOTBALL_SOURCE` dans `youtube.ts` — vraies miniatures (`i.ytimg.com`) affichées via prop `image` de `Thumb`/`VideoPlayer`.

**Football = source externe :** les lives foot LSEI sont diffusés sur la chaîne de la Ligue (« LBF TV », @liguebretagne — PAS la chaîne LSEI). Décision : football alimenté par une **liste de liens de vidéos curatée** (l'utilisateur donne les liens). Métadonnées (titre, miniature) récupérables via l'**oEmbed public YouTube** (`youtube.com/oembed`) → pas besoin de clé API pour le foot. En attente des liens de l'utilisateur. Réseaux réels (composant `SocialLinks.astro`, header compact + footer) : YouTube @LSEIWebTvSport, Facebook LSEI.LESPORTENIMAGE, Instagram lsei_web_tv_sport, TikTok @lseiwebtvsport. Barre du haut : icônes réseaux uniquement (S'abonner/Newsletter/bouton Thème retirés). CTA = un seul bouton « Nous contacter ».

**API YouTube BRANCHÉE et fonctionnelle (2026-07-15) :** clé dans `.env` (restriction application = « Aucune », API = YouTube Data v3 — le référent HTTP bloquait). `youtube.ts` implémente le vrai fetch (playlists → `classifyPlaylist` → playlistItems), avec repli sur mock en cas d'erreur. Résultat réel : billard 538, basket 141, palets 51, beach 14, bowling 11, football 12 (curaté), subbuteo 0 (playlist à créer) — ~767 vidéos. Hero = dernier live si en cours, sinon dernière vidéo (« À revoir »). Serveur dev relancé après ajout de la clé (Vite lit `.env` au démarrage). Reste à polir : titres bruts YouTube (emojis, « 25/26 - »), pagination des pages disciplines très longues (billard 538), déploiement, éventuelle rotation de la clé (passée dans le chat).

**Quota YouTube — leçon importante (2026-07-15) :** NE PAS utiliser `search.list` (100 unités/appel + quota « Search Queries » quotidien minuscule → épuisé pendant les tests, faisait retomber le site sur la démo). Détection direct/programmé réécrite SANS search : `channels.list` → playlist « uploads » → `playlistItems` → `videos.list(part=liveStreamingDetails)` (~3 unités). `fetchLiveState()` dans `youtube.ts` (cache par build). `getTicker`/`getProgramme` renvoient `[]` si pas de direct ; `getLiveFeature` = dernier direct sinon dernière vidéo (jamais MOCK sur site réel). Rail accueil « Dernières vidéos » affiche des miniatures (`.prog-thumb`). Pagination disciplines + vidéothèque = 24/page (routes `[slug]/[...page]` et `videotheque/[...page]`). Responsive validé (mobile 375, tablette 768, desktop — 0 débordement). Hébergeur pressenti : OVH (déploiement en pause, l'utilisateur inspecte encore).

**Direct (2026-07-15) :** page `/direct` avec lecteur **temps réel** via l'embed `youtube.com/embed/live_stream?channel=<channelId>` (façade click-to-load, RGPD) → montre le live à la seconde où il démarre, SANS reconstruction. Bouton « Regarder le direct » (header + mobile) → `/direct`. `channelId` public dans `site.ts`. Directs **programmés** : détectés via `getProgramme`/`fetchLiveState` (uploads playlist, `scheduledStartTime`) → affichés dans « Au programme » et sur `/direct`, mais seulement après une reconstruction (OK car programmés à l'avance ; doivent être PUBLICS). Hero accueil : lecture **en place** (composant `VideoPlayer` avec `id`/`live`/`score`, plus de navigation vers `/replay`). `LiveFeature.id` ajouté.

**Tri par heure de diffusion (2026-07-15) :** `fetchRealReplays` trie désormais par `liveStreamingDetails.actualStartTime` (heure RÉELLE de diffusion du live), sinon `videoPublishedAt`. Raison : la date de publication YouTube est mélangée pour les lives (ex. finale palet diffusée en dernier mais « publiée » avant d'autres segments → elle n'était pas en tête). Coût : ~16 appels `videos.list?part=liveStreamingDetails` (batch 50). Résultat : le dernier match diffusé apparaît en premier partout (hero, rail, disciplines, vidéothèque). Clic sur une vidéo du rail accueil → lecture dans le grand lecteur en place (script inline dans `index.astro`). Note : beaucoup de vidéos LSEI ont des titres identiques (segments d'un même live) — distinguables surtout par la miniature.

**Saisons (2026-07-15) :** saison déduite de la date de diffusion (logique août→juillet, `seasonOf`), champ `Video.season` (ex. "2025/2026"). Filtre par « chips » élégants (`SeasonNav.astro` : saison courte 25/26 + nb de vidéos, teinte configurable) sur vidéothèque (cobalt) ET pages disciplines (couleur du sport). Routes : `videotheque/saison/[season]/[...page]` et `disciplines/[slug]/saison/[season]/[...page]`. `getSeasonCounts(discipline?)`, `seasonShort`. Grille factorisée dans `VideoGrid.astro`. Filtre par **propriétaire de chaîne** ajouté (`videoOwnerChannelId !== CHANNEL` ignoré) → 3 vidéos étrangères retirées (ex. « Best of Led Zeppelin » dans une playlist). Build ~921 pages.

**Filtres = menus déroulants (2026-07-15) :** l'utilisateur a trouvé les chips « très vilaines » (surtout 2 rangées sur billard). Remplacées par des `<select>` compacts (`FilterMenu.astro`, onchange → navigation) pour saison ET catégorie, sur toutes les pages. `SeasonNav.astro` supprimé. Option « Toutes … » remise en tête du select. CSS `.filter-bar`/`.filter-menu`.

**Catégories billard (2026-07-15) :** filtre « Catégories » (par compétition) UNIQUEMENT sur le billard (choix user). `Video.category` déduit par mots-clés du titre de playlist (`billardCategory` dans `youtube.ts`), 9 catégories + « Autres » (`BILLARD_CATEGORIES`). Route `disciplines/billard/categorie/[cat]/[...page]`. `getBillardCategories`, `categorySlug`/`categoryFromSlug`, `slugify` (remplacement d'accents explicite). `SeasonNav` a un prop `label` (réutilisé pour « Catégories »). LIMITE connue : classification approximative (dédup 1ère playlist → « Femmes » sous-compté, absorbé dans « Tournois Nationaux » ; « Autres » = playlists sans mot-clé, ex. FBEP/Fédération). À affiner avec l'aide de l'utilisateur si besoin. Build ~947 pages.

**Couverture d'article auto (2026-07-16) :** `src/lib/articles.ts` → `articleCover()` renvoie `{src, fallback?}`. Priorité : `cover` manuel > `coverFrame` (enum default/start/middle/end → maxresdefault|hq1|hq2|hq3.jpg) > maxresdefault (repli hqdefault via `onerror`). Champ `coverFrame` dans le schéma + select dans l'admin Decap. Utilisé dans liste, page article, accueil (avec `onerror` de repli). Note : remark plugin `remark-youtube.mjs` rendu robuste (tout paragraphe contenant un lien YouTube → lecteur, même avec texte avant). Changement `content.config.ts`/`astro.config.mjs` → redémarrer `npm run dev`.

**Vidéo dans les articles (2026-07-16) :** plugin `src/lib/remark-youtube.mjs` (dep `unist-util-visit`) branché dans `astro.config.mjs` (`markdown.remarkPlugins`) → un lien YouTube seul sur une ligne dans un `.md` devient une façade « clic pour lire » (`.article-video`/`.yt-facade`, handler dans `VideoModal.astro`). Bouton « Vidéo YouTube » ajouté dans l'éditeur CMS (`registerEditorComponent` dans `public/admin/index.html`). Changement `astro.config` → redémarrer `npm run dev`.

**Éditeur visuel Quill + couleur (2026-07-16) :** l'utilisateur voulait « sélection → couleur » (impossible avec le widget markdown Decap). Solution : widget Decap custom `richtext` (Quill 1.3.7 via CDN, toolbar avec `{color:[]}`) dans `public/admin/index.html` (via `window.h`/`window.createClass` — disponibles). `config.yml` body → `widget: richtext`. **Corps d'article = HTML** (plus markdown). Rendu : `article.body` → `renderArticleBody()` (dans `articles.ts`, convertit iframes/liens YouTube en façades) → `set:html` (plus de `render()`). Les 2 articles d'exemple convertis en HTML. `.prose` : ajout h3/u. Testé : éditeur charge + palette couleur OK dans l'admin, rendu site OK, build 950 pages. Remark-youtube.mjs devient inutile pour les articles HTML mais reste inoffensif.

**CMS local OPÉRATIONNEL (2026-07-16) :** `decap-server` (devDep) + script `npm run cms` (port 8081). Avec `npm run dev` + `npm run cms`, l'admin `/admin/index.html` fonctionne : login local → collection Actualités, création/édition d'articles → écrit dans `src/content/articles/`. Images → `public/uploads/`. Testé OK. Reste : version web en ligne (backend Git + auth) au déploiement.

**Articles / Actualités (2026-07-16) :** collection de contenu Astro (`src/content.config.ts`, glob loader, `src/content/articles/*.md`), schéma {title, date, excerpt, cover, discipline?, author, draft}. Pages `actualites/index` (liste) + `actualites/[slug]` (article, `render()` du markdown). Section « Actualités » sur l'accueil (3 dernières). Menu = Direct · Disciplines · **Actualités** (Vidéothèque + La web TV retirées du menu, restent au footer ; lien rail accueil → /disciplines). CMS web préparé : Decap CMS dans `public/admin/` (index.html + config.yml), `local_backend: true` pour test via `npx decap-server`. **Backend Git + auth à finaliser au DÉPLOIEMENT** (dépend d'OVH/hébergeur Git). Dev : `/admin/` ne résout pas l'index → utiliser `/admin/index.html` (OK en prod). 2 articles d'exemple. Build ~950 pages.

**Fait (2026-07-15) :** page match/replay `replay/[id]` (lecteur façade « click-to-load » YouTube nocookie, respect RGPD, + vidéos liées) · menu mobile (hamburger dans `Header.astro`) + finitions responsive vérifiées à 375px · pages légales `mentions-legales` + `confidentialite` (placeholders `[à compléter]` dans `legal` de `site.ts`) · bannière cookies (`CookieBanner.astro`, aucun cookie déposé par défaut) · liens légaux dans le footer.

**Reste à faire :** brancher le réel (clé API + channel ID + playlists), vrai logo, vraies miniatures, page À propos/Le club, déploiement sur `lsei.tv`. Captures d'écran de l'outil navigateur KO dans cet environnement → vérifier via `read_page`/`javascript_tool` ou ouvrir `localhost:4321`.
